// Reserved for future native integrations (e.g. local file drag-and-drop for
// Excel/PDF uploads, native print dialogs for exported ledgers). Kept minimal
// and context-isolated - the renderer only ever talks to the backend over
// normal HTTPS fetch calls, same as the web dashboard.
